---
layout: layouts/tool.njk
title: URL Encoder & Decoder
description: Free online URL encoder and decoder. Encode special characters for URLs or decode URL-encoded strings. Essential for web development and API work.
keywords: URL encoder, URL decoder, percent encoding, URI encode, URI decode, encodeURIComponent, decodeURIComponent, URL escape
permalink: /tools/url-encoder/
toolId: url-encoder
toolJs: url-encoder.js
ogImage: url-encoder.jpg
---
{# Get tool data from tools.json #}
{% set toolData = tools | findById(toolId) %}

{% block schema %}
<!-- Schema.org BreadcrumbList -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {"@type": "ListItem", "position": 1, "name": "Home", "item": "{{ site.url }}/"},
    {"@type": "ListItem", "position": 2, "name": "Tools", "item": "{{ site.url }}/#tools"},
    {"@type": "ListItem", "position": 3, "name": "{{ toolData.name }}", "item": "{{ site.url }}{{ page.url }}"}
  ]
}
</script>

<!-- Schema.org WebApplication -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "{{ toolData.fullName }}",
  "description": "{{ description }}",
  "url": "{{ site.url }}{{ page.url }}",
  "applicationCategory": "DeveloperApplication",
  "operatingSystem": "Any",
  "browserRequirements": "Requires JavaScript",
  "offers": {"@type": "Offer", "price": "0", "priceCurrency": "USD"},
  "author": {"@type": "Organization", "name": "{{ site.name }}", "url": "{{ site.url }}"}
}
</script>
{% endblock %}

{% block head %}
<style>
  .encoding-mode { display: flex; gap: var(--space-2); margin-bottom: var(--space-4); }
  .encoding-mode-btn { flex: 1; }
  .url-stats { display: flex; gap: 1.5rem; flex-wrap: wrap; font-size: 0.875rem; color: var(--color-text-muted); }
  .url-stat-item { display: flex; flex-direction: column; gap: 0.25rem; }
  .url-stats dt { font-weight: 500; color: var(--color-text-secondary); }
  .url-stats dd { margin: 0; font-family: monospace; }
</style>
{% endblock %}

<div class="tool-container">
  <!-- Encoding Mode Toggle -->
  <div class="encoding-mode">
    <button type="button" class="btn-option encoding-mode-btn active" data-mode="component" id="mode-component">
      Component
      <small style="display: block; font-size: 0.75rem; font-weight: normal; opacity: 0.8;">encodeURIComponent</small>
    </button>
    <button type="button" class="btn-option encoding-mode-btn" data-mode="full" id="mode-full">
      Full URL
      <small style="display: block; font-size: 0.75rem; font-weight: normal; opacity: 0.8;">encodeURI</small>
    </button>
  </div>

  <!-- Input Section -->
  <div class="tool-input-group">
    <div class="input-header">
      <label for="url-input" class="input-label">Enter Text or URL</label>
      <button type="button" id="sample-url" class="btn btn-text btn-sm">Load Sample</button>
    </div>
    <textarea id="url-input" class="tool-textarea" rows="6" placeholder="Enter text to encode or URL-encoded string to decode..." spellcheck="false"></textarea>
  </div>

  <!-- Action Buttons -->
  <div class="tool-actions">
    <button type="button" id="encode-url" class="btn btn-primary">Encode</button>
    <button type="button" id="decode-url" class="btn btn-secondary">Decode</button>
    <button type="button" id="copy-url" class="btn btn-secondary">Copy</button>
    <button type="button" id="clear-url" class="btn btn-ghost">Clear</button>
  </div>

  <!-- Error Display -->
  <div id="url-error" class="hidden" role="alert" style="background: #fef2f2; border: 1px solid #fecaca; color: #b91c1c; padding: 0.75rem; border-radius: 0.375rem; margin-top: 0.5rem;"></div>

  <!-- Output Section -->
  <div class="tool-output-group">
    <div class="output-header">
      <span class="output-label">Output</span>
      <div class="url-stats">
        <div class="url-stat-item"><dt>Input Length</dt><dd id="input-length">-</dd></div>
        <div class="url-stat-item"><dt>Output Length</dt><dd id="output-length">-</dd></div>
      </div>
    </div>
    <pre id="url-output" class="tool-output tool-output-placeholder">Encoded or decoded result will appear here</pre>
  </div>
</div>

<!-- How to Use Section -->
<section class="content-section">
  <h2>How to Use</h2>
  <div class="steps">
    <div class="step"><div class="step-number">1</div><div class="step-content"><h3>Enter Your Text</h3><p>Type or paste the text you want to encode, or paste a URL-encoded string to decode.</p></div></div>
    <div class="step"><div class="step-number">2</div><div class="step-content"><h3>Choose Encoding Mode</h3><p><strong>Component</strong> encodes all special characters (for query params). <strong>Full URL</strong> preserves URL structure characters.</p></div></div>
    <div class="step"><div class="step-number">3</div><div class="step-content"><h3>Encode or Decode</h3><p>Click "Encode" to convert to URL format, or "Decode" to convert back to readable text.</p></div></div>
  </div>
</section>

<!-- FAQ Section -->
<section class="content-section">
  <h2>FAQ</h2>
  <div class="faq-list">
    <details class="faq-item">
      <summary class="faq-question">What is URL encoding?</summary>
      <div class="faq-answer"><p>URL encoding (percent encoding) replaces special characters with a % followed by their hexadecimal value. For example, a space becomes %20. This ensures URLs are valid and can be transmitted safely.</p></div>
    </details>
    <details class="faq-item">
      <summary class="faq-question">What's the difference between Component and Full URL mode?</summary>
      <div class="faq-answer"><p><strong>Component mode</strong> (encodeURIComponent) encodes everything except A-Z, a-z, 0-9, and - _ . ~. Use this for query parameter values.<br><strong>Full URL mode</strong> (encodeURI) also preserves : / ? # [ ] @ ! $ & ' ( ) * + , ; = for valid URL structure.</p></div>
    </details>
    <details class="faq-item">
      <summary class="faq-question">Is my data secure?</summary>
      <div class="faq-answer"><p>Yes. All encoding/decoding happens in your browser. Your data never leaves your device.</p></div>
    </details>
  </div>
</section>

<!-- Related Tools -->
<section class="content-section">
  <h2>Related Tools</h2>
  <div class="related-tools">
    {% for tool in tools %}
    {% if tool.id != toolId %}
    <a href="/tools/{{ tool.id }}/" class="related-tool-link">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" width="16" height="16">{{ tool.icon | safe }}</svg>
      {{ tool.name }}
    </a>
    {% endif %}
    {% endfor %}
  </div>
</section>
