---
layout: layouts/tool.njk
title: Password Generator
description: Generate secure, random passwords instantly. Customize length, symbols, numbers & more. Free online password generator with strength meter.
keywords: password generator, random password, secure password, strong password, password creator, online password generator
permalink: /tools/password-generator/
toolId: password-generator
toolJs: password-generator.js
ogImage: password-generator.jpg
---
{% set toolData = tools | findById(toolId) %}

{% block schema %}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {"@type": "ListItem", "position": 1, "name": "Home", "item": "{{ site.url }}/"},
    {"@type": "ListItem", "position": 2, "name": "Tools", "item": "{{ site.url }}/#tools"},
    {"@type": "ListItem", "position": 3, "name": "{{ toolData.name }}", "item": "{{ site.url }}{{ page.url }}"}
  ]
}
</script>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "{{ toolData.fullName }}",
  "description": "{{ description }}",
  "url": "{{ site.url }}{{ page.url }}",
  "applicationCategory": "SecurityApplication",
  "operatingSystem": "Any",
  "browserRequirements": "Requires JavaScript",
  "offers": {"@type": "Offer", "price": "0", "priceCurrency": "USD"},
  "featureList": ["Cryptographically secure random generation", "Customizable password length", "Include/exclude character types", "Password strength indicator", "Bulk generation"],
  "author": {"@type": "Organization", "name": "{{ site.name }}", "url": "{{ site.url }}"}
}
</script>
{% endblock %}

{% block head %}
<style>
  .strength-meter { margin-top: var(--space-4); }
  .strength-bar-container { height: 8px; background: var(--color-border); border-radius: var(--radius-full); overflow: hidden; }
  .strength-bar { height: 100%; width: 0%; transition: width 0.3s ease, background-color 0.3s ease; border-radius: var(--radius-full); }
  .strength-label { font-size: var(--text-sm); font-weight: 500; margin-top: var(--space-2); display: block; }
  .password-display { font-family: var(--font-mono); font-size: var(--text-lg); word-break: break-all; letter-spacing: 0.05em; }
  .options-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: var(--space-3); margin-top: var(--space-4); }
  .length-control { display: flex; align-items: center; gap: var(--space-4); }
  .length-control input[type="range"] { flex: 1; }
  .length-value { min-width: 50px; text-align: center; font-weight: 600; font-size: var(--text-lg); color: var(--color-primary); }
</style>
{% endblock %}

<div class="tool-container">
  <div class="tool-output" aria-live="polite">
    <div id="password-output" class="tool-output-text password-display tool-output-placeholder">Click "Generate" to create a password</div>
  </div>

  <div class="strength-meter">
    <div class="strength-bar-container">
      <div id="strength-bar" class="strength-bar"></div>
    </div>
    <span id="strength-label" class="strength-label">-</span>
  </div>

  <div class="tool-actions">
    <button id="generate-password" class="btn btn-primary">Generate Password</button>
    <button id="copy-password" class="btn btn-secondary">Copy</button>
  </div>

  <div style="margin-top: var(--space-6); padding-top: var(--space-6); border-top: 1px solid var(--color-border);">
    <h3 style="margin-top: 0; margin-bottom: var(--space-4); font-size: var(--text-base);">Password Options</h3>
    <div class="tool-input-group">
      <label for="password-length">Password Length</label>
      <div class="length-control">
        <input type="range" id="password-length" min="4" max="128" value="16">
        <span id="length-display" class="length-value">16</span>
      </div>
      <small style="color: var(--color-text-muted); font-size: var(--text-xs);">Recommended: 16+ characters</small>
    </div>
    <div class="options-grid">
      <label class="checkbox-label"><input type="checkbox" id="include-uppercase" class="password-option" checked> Uppercase (A-Z)</label>
      <label class="checkbox-label"><input type="checkbox" id="include-lowercase" class="password-option" checked> Lowercase (a-z)</label>
      <label class="checkbox-label"><input type="checkbox" id="include-numbers" class="password-option" checked> Numbers (0-9)</label>
      <label class="checkbox-label"><input type="checkbox" id="include-symbols" class="password-option" checked> Symbols (!@#$%...)</label>
      <label class="checkbox-label"><input type="checkbox" id="exclude-ambiguous" class="password-option"> Exclude Ambiguous (il1Lo0O)</label>
    </div>
  </div>

  <div style="margin-top: var(--space-6); padding-top: var(--space-6); border-top: 1px solid var(--color-border);">
    <h3 style="margin-top: 0; margin-bottom: var(--space-4); font-size: var(--text-base);">Bulk Generation</h3>
    <div class="tool-input-group" style="max-width: 150px;">
      <label for="bulk-password-count">Count (1-20)</label>
      <input type="number" id="bulk-password-count" min="1" max="20" value="5">
    </div>
    <div class="tool-actions" style="margin-top: var(--space-3);">
      <button id="bulk-generate-passwords" class="btn btn-secondary">Generate Bulk</button>
      <button id="copy-all-passwords" class="btn btn-secondary">Copy All</button>
    </div>
    <pre id="bulk-passwords-output" class="tool-output hidden" style="margin-top: var(--space-4); max-height: 300px; overflow-y: auto;"></pre>
  </div>
</div>

<section class="content-section">
  <h2>How to Use</h2>
  <div class="steps">
    <div class="step"><div class="step-number">1</div><div class="step-content"><h3>Set Length</h3><p>Use the slider to choose between 4-128 characters. We recommend at least 16 characters.</p></div></div>
    <div class="step"><div class="step-number">2</div><div class="step-content"><h3>Choose Character Types</h3><p>Select which characters to include - uppercase, lowercase, numbers, and symbols.</p></div></div>
    <div class="step"><div class="step-number">3</div><div class="step-content"><h3>Generate & Copy</h3><p>Click "Generate Password" and then "Copy" to copy to your clipboard.</p></div></div>
  </div>
</section>

<section class="content-section">
  <h2>FAQ</h2>
  <div class="faq-list">
    <details class="faq-item">
      <summary class="faq-question">How secure are the generated passwords?</summary>
      <div class="faq-answer"><p>Our generator uses the browser's cryptographic API (crypto.getRandomValues) for true randomness. Passwords are generated locally and never sent to any server.</p></div>
    </details>
    <details class="faq-item">
      <summary class="faq-question">What makes a strong password?</summary>
      <div class="faq-answer"><p>A strong password is at least 12-16 characters long with a mix of uppercase, lowercase, numbers, and symbols. Avoid personal information and common words.</p></div>
    </details>
    <details class="faq-item">
      <summary class="faq-question">Is my data secure?</summary>
      <div class="faq-answer"><p>Yes. All processing happens in your browser. Your passwords never leave your device.</p></div>
    </details>
  </div>
</section>

<section class="content-section">
  <h2>Related Tools</h2>
  <div class="related-tools">
    {% for tool in tools %}
    {% if tool.id != toolId %}
    <a href="/tools/{{ tool.id }}/" class="related-tool-link">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" width="16" height="16">{{ tool.icon | safe }}</svg>
      {{ tool.name }}
    </a>
    {% endif %}
    {% endfor %}
  </div>
</section>
