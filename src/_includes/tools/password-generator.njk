{# Password Generator Tool UI Partial #}
<style>
  .strength-meter { margin-top: var(--space-4); }
  .strength-bar-container { height: 8px; background: var(--color-border); border-radius: var(--radius-full); overflow: hidden; }
  .strength-bar { height: 100%; width: 0%; transition: width 0.3s ease, background-color 0.3s ease; border-radius: var(--radius-full); }
  .strength-label { font-size: var(--text-sm); font-weight: 500; margin-top: var(--space-2); display: block; }
  .password-display { font-family: var(--font-mono); font-size: var(--text-lg); word-break: break-all; letter-spacing: 0.05em; }
  .options-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: var(--space-3); margin-top: var(--space-4); }
  .length-control { display: flex; align-items: center; gap: var(--space-4); }
  .length-control input[type="range"] { flex: 1; }
  .length-value { min-width: 50px; text-align: center; font-weight: 600; font-size: var(--text-lg); color: var(--color-primary); }
</style>

<div class="tool-container">
  <div class="tool-output" aria-live="polite">
    <div id="password-output" class="tool-output-text password-display tool-output-placeholder">{{ "common.outputPlaceholder" | t(lang) }}</div>
  </div>

  <div class="strength-meter">
    <div class="strength-bar-container">
      <div id="strength-bar" class="strength-bar"></div>
    </div>
    <span id="strength-label" class="strength-label">-</span>
  </div>

  <div class="tool-actions">
    <button id="generate-password" class="btn btn-primary">{{ "common.generate" | t(lang) }} Password</button>
    <button id="copy-password" class="btn btn-secondary">{{ "common.copy" | t(lang) }}</button>
  </div>

  <div style="margin-top: var(--space-6); padding-top: var(--space-6); border-top: 1px solid var(--color-border);">
    <h3 style="margin-top: 0; margin-bottom: var(--space-4); font-size: var(--text-base);">Password {{ "common.options" | t(lang) }}</h3>
    <div class="tool-input-group">
      <label for="password-length">Password {{ "common.length" | t(lang) }}</label>
      <div class="length-control">
        <input type="range" id="password-length" min="4" max="128" value="16">
        <span id="length-display" class="length-value">16</span>
      </div>
      <small style="color: var(--color-text-muted); font-size: var(--text-xs);">Recommended: 16+ characters</small>
    </div>
    <div class="options-grid">
      <label class="checkbox-label"><input type="checkbox" id="include-uppercase" class="password-option" checked> {{ "common.includeUppercase" | t(lang) }}</label>
      <label class="checkbox-label"><input type="checkbox" id="include-lowercase" class="password-option" checked> {{ "common.includeLowercase" | t(lang) }}</label>
      <label class="checkbox-label"><input type="checkbox" id="include-numbers" class="password-option" checked> {{ "common.includeNumbers" | t(lang) }}</label>
      <label class="checkbox-label"><input type="checkbox" id="include-symbols" class="password-option" checked> {{ "common.includeSymbols" | t(lang) }}</label>
      <label class="checkbox-label"><input type="checkbox" id="exclude-ambiguous" class="password-option"> {{ "common.excludeAmbiguous" | t(lang) }}</label>
    </div>
  </div>

  <div style="margin-top: var(--space-6); padding-top: var(--space-6); border-top: 1px solid var(--color-border);">
    <h3 style="margin-top: 0; margin-bottom: var(--space-4); font-size: var(--text-base);">{{ "common.bulkGenerate" | t(lang) }}</h3>
    <div class="tool-input-group" style="max-width: 150px;">
      <label for="bulk-password-count">{{ "common.count" | t(lang) }} (1-20)</label>
      <input type="number" id="bulk-password-count" min="1" max="20" value="5">
    </div>
    <div class="tool-actions" style="margin-top: var(--space-3);">
      <button id="bulk-generate-passwords" class="btn btn-secondary">{{ "common.bulkGenerate" | t(lang) }}</button>
      <button id="copy-all-passwords" class="btn btn-secondary">{{ "common.copyAll" | t(lang) }}</button>
    </div>
    <pre id="bulk-passwords-output" class="tool-output hidden" style="margin-top: var(--space-4); max-height: 300px; overflow-y: auto;"></pre>
  </div>
</div>
